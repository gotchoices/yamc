#!/bin/bash
# k/setup.loc - Prepare K editor installation locally

# The setup script clones directly from GitHub, so no local resources needed
# except for the termcap file which is included in the module

echo "K editor setup: will clone from https://github.com/gotchoices/k.git"
